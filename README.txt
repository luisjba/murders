We analyze US gun murder data colleted by the FBI.

donwload-data.R  - donwloads csv file to data directory

wrangle-data.R - creates a derived dataset and saves as R objects un rda directory

analysis.R - generates a plot and saves it ti the figs directory